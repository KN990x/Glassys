# Deliberately not on push or pull_request. A vulnerability published today is not a
# reason to block a commit that has nothing to do with it: run on every push, it would sit
# red on main and decorate every Dependabot PR with a cross nobody could clear.
#
# Monthly, not weekly: Dependabot alerts already arrive the day an advisory is published,
# from the same database. This is the backstop, and a weekly one only repeated the same
# failure email every Monday until the fix landed.
name: Security audit

on:
  schedule:
    # The 1st of each month, 03:30 UTC: before Dependabot's monthly run (06:00 Madrid).
    - cron: "30 3 1 * *"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  audit:
    name: pnpm audit
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # No `version:` — pnpm/action-setup reads the one pinned by `packageManager`.
      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ".nvmrc"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # Blocking: these are the packages that ship with the gateway and the adapters.
      - name: Audit production dependencies
        run: pnpm audit --prod --audit-level high

      # Informational: a vulnerable linter or test runner never reaches an operator, so it
      # reports without failing the job.
      - name: Audit the full tree (informational)
        continue-on-error: true
        run: pnpm audit --audit-level moderate

  # Dependabot ignores majors on purpose; this keeps them visible in one issue that is
  # edited in place (no notification) instead of a PR per breaking release.
  majors:
    name: Pending majors
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - uses: pnpm/action-setup@ea17c68df8912ef543352723c149a84f56e3d413 # v6.1.0

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: ".nvmrc"
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      # Nothing is executed here, so no install scripts either.
      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Rewrite the "Pending major updates" issue
        uses: KN990x/.github/actions/majors-report@716c418539e8af359ae2916399a155b9d00d11c5 # v1.0.0
